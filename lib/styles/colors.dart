import 'package:flutter/material.dart';

class AppColors {
  // Base
  static const Color bg = Color(0xFF0B1220); // page background
  static const Color surface = Color(0xFF111827); // panels/cards
  static const Color surface2 = Color(0xFF0F172A); // deeper panel
  static const Color outline = Color(0xFF293241);

  // Text
  static const Color text = Color(0xFFE5E7EB);
  static const Color textDim = Color(0xFF9CA3AF);

  // Seed / accents (screenshot shows multiple colored tags)
  static const Color seed = Color(0xFF60A5FA); // soft blue
  static const Color blue = Color(0xFF60A5FA);
  static const Color purple = Color(0xFFA78BFA);
  static const Color green = Color(0xFF34D399);
  static const Color orange = Color(0xFFF59E0B);
  static const Color red = Color(0xFFEF4444);
}
